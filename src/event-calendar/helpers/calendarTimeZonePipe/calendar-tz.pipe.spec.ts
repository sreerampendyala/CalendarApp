import { CalendarTZPipe } from './calendar-tz.pipe';

describe('CalendarTZPipe', () => {
  it('create an instance', () => {
    const pipe = new CalendarTZPipe();
    expect(pipe).toBeTruthy();
  });
});
