import { CalendarScrollDirective } from './calendar-scroll.directive';

describe('CalendarScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new CalendarScrollDirective();
    expect(directive).toBeTruthy();
  });
});
